// 阶段接力与系统回合指令集中地：接力协议块、接力按钮指令、标题重命名指令。
// 纯字符串模块（不 import electron）。注意：delegate.ts 的 CONTINUE_EXAMPLE_FINGERPRINT
// 防复述指纹与下方示例简报逐字联动——改示例必须同步改指纹与 smoke-continue.mjs 的断言。

/** 阶段接力协议：多阶段任务在阶段边界输出 <continue>，系统在同一 Issue 上硬切新会话 */
export const CONTINUE_BLOCK = `【阶段接力（分阶段任务使用）】
本任务分阶段施工、且下一阶段的目标已明确时，在回复的最后一行输出接力标记：
<continue start="auto">下一阶段简报</continue>
位置要求：标记必须是整个回复的结尾（其后最多跟空白）——末尾锚定是识别接力的主通道；不在末尾的标记只有显式写 start="auto" 才会被兜底识别。正文、示例或讨论里出现标记字样不会触发接力。
简报要求：必须自包含——阶段目标、方案文档路径、上阶段成果（commit/关键文件:行号）、约束与验收。接手的会话看不到本会话上下文，一切靠这份简报。
start 语义：想让系统立即续跑就必须显式写 start="auto"；缺省或写错一律按 start="parked" 停放，等你手动点「▶ 启动」。
- start="parked" 只用于真实阶段边界且你明确要求用户手动启动下一阶段的情形，并且必须在正文里写明"我停在阶段 N，等你启动下一阶段"；若只是等待用户确认方案、补充信息或提供凭据，留在当前会话等待，不要输出 parked 后继。
- 环境受限（无头跑不了 GUI、缺权限、缺依赖）不算等用户的理由——照常 auto，把风险与规避办法写进简报，让下一阶段自行绕开。
- 阶段内或拿不准是否切会话时：留在当前会话继续执行；若需要用户确认或补充信息，也在当前会话说明等待点并等待回复，不要为了“备好待启”生成 parked 后继。
- 真实阶段边界才允许接力：当前阶段已完成、下一阶段目标明确时，按需要输出 start="auto"（立即接手）或 start="parked"（明确要求用户手动启动）。接手后先执行简报所对应的当前阶段；同一阶段不得反复交接，只有完成当前阶段并跨过新的阶段边界后才能再次输出接力标记。
- 示例：<continue start="auto">阶段2：按 docs/plan.md §3 实现模型选择 UI；阶段1 已完成数据管道（commit 09a47a4，src/main/presets.ts）；验收：两个不同模型的 agent 并发执行成功</continue>`

/** 用户点「⇥ 接力下一阶段」按钮时注入的合成指令（显式人工入口；自由追问不再按关键词猜测意图） */
export const HANDOFF_CUE = `【系统】用户已通过「接力下一阶段」按钮确认进入下一阶段。请按【阶段接力】协议，在回复的最后一行输出 <continue start="auto">…</continue> 标记：简报必须自包含（下一阶段目标、方案文档/计划路径、本阶段成果与 commit、关键文件:行号、约束与验收——接手会话看不到本会话上下文）。若确实不存在明确的下一阶段，直接说明原因，不要输出标记。`

export const HANDOFF_RECEIVE_CUE = `【系统·接力接收】当前任务已经是上一阶段创建的后继。请先处理简报指定的当前阶段，不要仅复述简报或再次把同一阶段交出去。简报中记载的上一轮只读核查、未修改、未测试属于历史成果；当前任务明确限定的只读范围仍须遵守。需要确认或缺少信息时留在当前会话说明，不创建同阶段后继。`

export const HANDOFF_START_CONFIRMED_CUE = `【系统·手动启动确认】用户已通过启动操作确认开始本阶段；无需再次索取同一阶段的启动确认。该确认仅解除等待启动本阶段的前置条件，不批准任何工具权限、发布或数据迁移，不表示缺失凭据已经提供，也不解除任务明确限定的只读范围。其他审批和信息缺口仍需分别处理。`

/** 隐藏重命名回合：让 agent 根据执行内容重起简短标题（约束越短越硬越好，防标题带装饰） */
export const RETITLE_PROMPT = '【系统】根据这次任务的执行内容重起一个简短标题：不超过 24 个字，概括实际做了什么，不复述指令原文。只输出标题本身——不带编号、引号、书名号或任何解释。'
