export { SidecarManager, SidecarProtocolError, SIDECAR_PROTOCOL_VERSION, SIDECAR_STATE_FILE } from '../sidecar'
export type { SidecarManagerOptions, SidecarSnapshot, SidecarState, SidecarStatus, SidecarSyncState } from '../sidecar'

